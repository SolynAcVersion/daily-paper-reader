---
title: "CML-Bench: A Framework for Evaluating and Enhancing LLM-Powered Movie Scripts Generation"
title_zh: CML-Bench：用于评估和增强LLM驱动的电影剧本生成的框架
authors: "Mingzhe Zheng, Dingjie Song, Guanyu Zhou, Jun You, Jiahao Zhan, Xuran Ma, Xinyuan Song, Ser-Nam Lim, Qifeng Chen, Harry Yang"
date: 2025-09-19
pdf: "https://openreview.net/pdf?id=5OXvNX9LWh"
tags: ["query:manga-drama"]
score: 7.0
evidence: 面向电影剧本生成的评估与增强框架，是短剧自动化创作中剧本环节的关键技术。
tldr: 本文针对大语言模型生成电影剧本时缺乏叙事深度和情感的问题，提出了CML-Bench评估与增强框架，并构建了包含电影标记语言摘要-内容对的数据集。通过分析真实剧本中的多镜头连续性和叙事结构，该框架帮助识别LLM剧本的不足并加以改进。实验证明该方法能有效提升LLM生成剧本的结构与情感表达，为自动化短剧创作中的剧本环节提供了坚实支撑。
source: ICLR-2026-Rejected-Public
selection_source: conference_retrieval
motivation: LLM生成电影剧本时缺乏叙事深度和情感表达，难以满足真正电影剧本的需求。
method: 构建了CML-Dataset数据集，并基于其内部结构分析设计出评估与增强LLM剧本生成的框架CML-Bench。
result: 通过实验验证，该框架能够有效识别和改善LLM剧本在连续性和叙事结构上的不足，提升生成质量。
conclusion: CML-Bench为电影剧本生成提供了新的评估标准与增强途径，可迁移到短剧自动化创作中。
---

## Abstract
Large Language Models (LLMs) have demonstrated remarkable proficiency in generating highly structured texts. However, while exhibiting a high degree of structural organization, movie scripts demand an additional layer of nuanced storytelling and emotional depth—the 'soul' of compelling cinema—that LLMs often fail to capture. To investigate this deficiency, we first curated CML-Dataset, a dataset comprising (summary, content) pairs for Cinematic Markup Language (CML), where 'content' consists of segments from esteemed, high-quality movie scripts and 'summary' is a concise description of the content. Through an in-depth analysis of the intrinsic multi-shot continuity and narrative structures within these authentic scripts, we identified three pivotal dimensions for quality assessment: Dialogue Coherence (DC), Character Consistency (CC), and Plot Reasonableness (PR). Informed by these findings, we propose the CML-Bench, featuring quantitative metrics across these dimensions. CML-Bench effectively assigns high scores to well-crafted, human-written scripts while concurrently pinpointing the weaknesses in screenplays generated by LLMs. To further validate our benchmark, we introduce CML-Instruction, a prompting strategy that provides detailed instructions on character dialogue and event logic to guide LLMs in generating more structured and cinematically sound scripts. Extensive experiments validate the effectiveness of our benchmark and demonstrate that LLMs guided by CML-Instruction generate higher-quality screenplays, with results aligned with human preferences. Our work offers a comprehensive framework for both evaluating and guiding LLMs in screenplay authoring.

---

## 论文详细总结（自动生成）

# CML-Bench：用于评估和增强LLM驱动的电影剧本生成的框架——详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：大语言模型（LLM）在生成高度结构化文本方面已展现出卓越能力，例如代码、法律文书、学术摘要等场景均有成熟应用。
- **核心问题**：电影剧本不同于一般结构化文本，它不仅需要清晰的结构，还需要**富有层次的叙事技巧（nicer storytelling）与情感深度（emotional depth）**——即优秀电影所谓的“灵魂”。LLM 在生成剧本时，往往能保持结构完整，却难以捕捉这种叙事与情感层面的精髓。
- **研究动机**：现有评估方式不足以诊断 LLM 在剧本生成上的“结构性有余、叙事性不足”这一缺陷。作者希望构建一个既能**客观评估 LLM 剧本质量**，又能**引导 LLM 改进剧本生成**的完整框架。
- **整体含义**：该工作填补了“电影剧本生成质量评估与增强”领域的空白，提供了一套可量化、可复现的评估基准与指令策略，同时为短剧自动化创作等下游应用提供了剧本环节的关键技术支撑。

## 2. 方法论：核心思想、关键技术细节与流程

### 核心思想

> 以真实优质电影剧本的**内在结构规律**（多镜头连续性、叙事结构）为基准，反向构建评估维度与增强策略，使 LLM 生成剧本在结构与叙事上更接近人类专业编剧的水平。

### 方法论流程（按文字描述）

1. **构建数据集 CML-Dataset**
   - 数据形态：由（summary, content）对组成的高质量剧本数据，其中：
     - **content**：取自受到好评的高质量电影剧本片段；
     - **summary**：对该片段内容的简洁描述。
   - 采用**电影标记语言（Cinematic Markup Language, CML）** 作为剧本的结构化表示形式。
   - 用途：通过对真实剧本中**多镜头连续性（multi-shot continuity）** 与**叙事结构（narrative structures）** 的深入分析，总结出剧本优劣的鉴别依据。

2. **识别三个关键质量评估维度**
   - **DC（Dialogue Coherence，对话连贯性）**：衡量角色对话的逻辑一致性与上下文衔接是否自然。
   - **CC（Character Consistency，角色一致性）**：衡量角色言行、性格、动机在剧情推进中是否保持一致。
   - **PR（Plot Reasonableness，情节合理性）**：衡量故事情节发展的逻辑是否自洽、因果关系是否成立、是否令人信服。

3. **提出评估基准 CML-Bench**
   - 在上述三个维度上设计定量评估指标。
   - **有效性验证方式**：人工撰写的优质剧本在 CML-Bench 上获得高分；LLM 生成的剧本则被准确识别出薄弱环节。

4. **引入增强策略 CML-Instruction**
   - 一种提示词（prompting）策略，向 LLM 提供**关于角色对话与事件逻辑的详细指令**，引导模型生成更具电影感、结构更严谨的剧本。
   - 与 CML-Bench 形成闭环：Bench 负责“诊断”，Instruction 负责“治疗”。

> **说明**：论文元数据中未提供形式化公式或算法伪代码，上述流程依据摘要内容以文字形式重构。

## 3. 实验设计

### 数据集/场景

| 组件 | 说明 |
|---|---|
| **CML-Dataset** | 包含 CML 格式的（摘要，内容）对，内容来源于优质电影剧本片段 |
| 评估场景 | 对人工撰写剧本与 LLM 生成剧本进行对比评估 |
| 生成场景 | 使用 CML-Instruction 引导 LLM 生成结构更合理的剧本 |

### Benchmark

- **CML-Bench**：基于 DC、CC、PR 三个维度构建的定量评估基准。

### 对比方法与验证策略

- **对比对象**：人类撰写的优质剧本 vs. LLM 直接生成的剧本。
- **引导对比**：LLM 直接生成 vs. LLM 在 CML-Instruction 指导下生成。
- **人类偏好对齐**：将 CML-Bench 的评分结果与人类评审的偏好进行对比，验证基准的客观有效性。

## 4. 资源与算力

- **未明确说明**：论文提供的元数据与摘要中，**未提及任何 GPU 型号、数量、训练时长、推理成本、数据集规模等具体算力信息**。
- 仅可确认实验涉及“大量实验（Extensive experiments）”，但具体的硬件环境与资源投入细节在现有材料中不可得。

## 5. 实验数量与充分性

- **数量**：摘要仅概括性地描述为“大量实验”，具体实验组数、数据集划分、消融设计等细节未在摘要中展开。
- **充分性评估**：
  - ✅ 已覆盖“评估有效性”（人类 vs. LLM 评分对比）与“增强有效性”（有/无 CML-Instruction 的生成质量对比）两条主线。
  - ✅ 引入人类偏好对齐，增强了评估结果的可信度。
  - ⚠️ 受限于现有信息，**无法确认是否包含消融实验**（如各维度独立贡献、CML-Instruction 各部分指令的消融）、多数据集泛化测试或跨语言/跨文化剧本的验证。
  - ⚠️ 公平性方面，目前未见与现有其他剧本评估方法的显式对比（如 BLEU、ROUGE、BERTScore 等），也未说明人类评估者的数量、专业背景与评分一致性指标。

## 6. 主要结论与发现

1. **LLM 剧本生成存在明确短板**：尽管 LLM 能生成结构完整的剧本，但在对话连贯性、角色一致性、情节合理性三个维度上均有不足，即缺乏“电影的灵魂”。
2. **CML-Bench 评估有效**：该基准能够高分评价优质的人类剧本，同时准确定位 LLM 生成剧本的弱点，说明其具有良好的判别能力。
3. **CML-Instruction 增强有效**：在该指令策略引导下，LLM 生成的剧本在 DC、CC、PR 维度上的质量显著提升。
4. **与人类偏好一致**：CML-Bench 的自动评估结果与人类主观偏好高度对齐，验证了该基准的可信度和实用价值。

## 7. 优点

- **问题定位精准**：敏锐地捕捉到 LLM 生成剧本“结构性有余、叙事性不足”这一容易被忽视的关键缺陷，研究动机有实际价值。
- **方法闭环设计**：评估基准（CML-Bench）与增强策略（CML-Instruction）形成完整的“诊断—治疗”闭环，而非只做评估或只做生成。
- **以真实数据为锚点**：基于高层次电影剧本的内在结构（多镜头连续性、叙事模式）构建评估维度，而非依赖主观经验或表面文本指标，保证了评估标准的专业性。
- **多维度量化评估**：DC、CC、PR 三个维度的划分使得评估结果具有可解释性，能够具体指出剧本在哪一环节薄弱。
- **人类偏好对齐验证**：将自动评估与人类主观判断进行对照，增强了基准本身的说服力与生态有效性。
- **应用前景广阔**：框架不仅适用于电影长片，也可迁移至短剧、短视频等自动化内容创作场景，具有实际产业化价值。

## 8. 不足与局限

- **算力与资源信息缺失**：论文未报告训练/推理所需的 GPU 资源、时间成本与数据集规模，不利于他人复现与成本估算。
- **实验细节不透明**：摘要中未提供具体实验配置、消融研究、统计显著性检验等信息，实验充分性难以完全判定。
- **评估维度覆盖面有限**：DC、CC、PR 三个维度虽重要，但电影剧本还涉及节奏控制、主题深度、视觉化程度、类型差异等因素，当前框架可能无法全面覆盖。
- **依赖 CML 格式**：基准基于电影标记语言（CML）构建，对于非 CML 格式的剧本或非文本形态的视听内容适用性存在限制。
- **文化/语言偏差风险**：数据来源于英文优质电影剧本，评估维度与指令策略可能隐含西方叙事传统偏好，对非英语或非西方叙事的剧本可能不友好。
- **提示策略的通用性存疑**：CML-Instruction 的设定是否对不同规模的 LLM（如小型开源模型 vs. 大型闭源模型）均有效，需要更多数据支撑。
- **未提供负面结果讨论**：未提及 CML-Bench 可能失效的场景或 CML-Instruction 产生负面影响的边界条件，客观性可进一步加强。

---

（完）
